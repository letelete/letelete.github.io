import { Testimonial } from '~features/testimonial';

export const TestimonialSection = () => {
  return (
    <div className='layout-width-limiter layout-padding flex w-full items-center justify-center'>
      <Testimonial />
    </div>
  );
};
